services:

  api:
    build: ./api
    ports:
      - "8000:8000"
    container_name: mnist_api

  frontend:
    build: ./frontend
    ports:
      - "5500:80"
    container_name: mnist_frontend
    depends_on:
      - api